# Supabase（启用真实跨端登录时填写；不提交真实密钥到仓库）
# 纯静态服务可在 index 前注入： window.__LUMINA_ENV__ = { VITE_SUPABASE_URL: "...", VITE_SUPABASE_ANON_KEY: "..." }
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 设为 1 时强制使用本地 demo 认证（忽略上方 URL/Key，用于本地与回归）
# VITE_LUMINA_AUTH_USE_DEMO=1
